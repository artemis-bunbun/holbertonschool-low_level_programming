#include "main.h"
/**
 * main - entry point
 * description: program 5-sign.c for task 5 - print_sign
 * Return: Always 0 (Success)
 */
int print_sign(int n)
{	if (n > 0)
	{		return (1);
	}
	else if (n == 0)
	{		return (0);
	}
	else	{		return (-1);
	}
}
