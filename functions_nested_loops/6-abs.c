#include "main.h"
/**
 * main - Entry point
 * Description: program for task 6 - absolute value
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
